{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Crafter",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/main.go",
            "cwd": "${workspaceFolder}",
            "buildFlags": "",
            "env": {}
        }
    ]
}
